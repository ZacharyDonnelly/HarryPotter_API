{
  "name": "harry_potter_api",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently 'cd packages/api && yarn dev' 'cd packages/web && yarn dev'"
  },
  "dependencies": {
    "concurrently": "^5.2.0"
  }
}
